create database tabledb;
use tabledb;
show tables;
create table employees(emp_id int primary key,   -- primary key
emp_name varchar(50) not null,                 -- not null
dept varchar(50),
salary int,            
city varchar(30) default 'coimbatore');
insert into employees values(1,'arun','IT',20000,'chennai'),
(2,'priya','ECE',25000,'madhurai'),
(3,'kumar','CSE',30000,'coimbatore'),
(4,'meena','AIDS',35000,default),
(5,'sara','AIML',40000,'chennai');
select * from employees;

select * from employees where city = 'chennai';  -- 1

select emp_name,salary from employees            -- 2
order by salary desc
limit 1;

select dept,count(*) as emmployee_count           -- 3
from employees 
group by dept;

insert into employees values(6,'kalim','IT',50000,'madhurai');

select * from employees where salary > 40000;      -- 4

select emp_name  from employees where emp_name like 'a%';     -- 5

select dept,avg(salary) as averagesalary            -- 6
from employees
group by dept;

select emp_name,salary from employees          -- 7
order by salary asc
limit 1;

select emp_name,dept from employees where dept<>'it';        -- 8

select city, COUNT(*)  AS employee_count 
FROM Employees 
GROUP BY city
having count(*) >=2 ;


select emp_name,salary as highest_salary
from employees                                        -- 10
order by salary desc
limit 3;




